@model IEnumerable<HotDogLover.DAL.HotDog>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LastAte)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LastPlaceAte)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LastAte)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LastPlaceAte)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.HotDogID }) |
            @Html.ActionLink("Details", "Details", new { id=item.HotDogID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.HotDogID })
        </td>
    </tr>
}

</table>
